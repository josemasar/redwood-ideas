# Migration `20210130223144-migration`

This migration has been generated by josemasar at 1/30/2021, 11:31:44 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Idea" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "opps" BOOLEAN NOT NULL,
    "partNum" TEXT NOT NULL,
    "product" TEXT NOT NULL,
    "system" TEXT NOT NULL,
    "vendor" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "body" TEXT NOT NULL,
    "value" INTEGER NOT NULL,
    "author" TEXT NOT NULL,
    "active" BOOLEAN NOT NULL DEFAULT false,
    "readyInMonth" INTEGER NOT NULL,
    "readyInYear" INTEGER NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);
INSERT INTO "new_Idea" ("id", "opps", "partNum", "product", "system", "vendor", "title", "body", "value", "author", "active", "createdAt") SELECT "id", "opps", "partNum", "product", "system", "vendor", "title", "body", "value", "author", "active", "createdAt" FROM "Idea";
DROP TABLE "Idea";
ALTER TABLE "new_Idea" RENAME TO "Idea";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210130215329-model-fixed..20210130223144-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -13,18 +13,19 @@
 // Define your own datamodels here and run `yarn redwood db save` to create
 // migrations for them.
 // TODO: Please remove the following example:
 model Idea {
-  id            Int      @id @default(autoincrement())
-  opps          Boolean
-  partNum       String
-  product       String
-  system        String
-  vendor        String
-  title         String
-  body          String
-  value         Int
-  author        String
-  active        Boolean  @default(false)
-  implementDate DateTime
-  createdAt     DateTime @default(now())
+  id           Int      @id @default(autoincrement())
+  opps         Boolean
+  partNum      String
+  product      String
+  system       String
+  vendor       String
+  title        String
+  body         String
+  value        Int
+  author       String
+  active       Boolean  @default(false)
+  readyInMonth Int
+  readyInYear  Int
+  createdAt    DateTime @default(now())
 }
```


